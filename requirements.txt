# 必須（合成データ・評価・numpy デモ）
numpy>=1.24

# PyTorch 実装（scripts/run_demo.py, src/biomodel/model.py, train.py）に必要。
# 環境によっては CPU ホイールの取得元を指定する:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# 実データ読込（任意; load_anndata / .h5ad を使う場合）
# anndata>=0.10

# テスト（任意）
pytest>=7.0
